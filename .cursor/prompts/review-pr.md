# Prompt template: Review a diff

Paste the diff after this prompt.

---

Review this diff against `docs/checklists/pr-self-review-checklist.md`, item by item.
Report violations of `.cursor/rules/01-platform-non-negotiables.mdc` or
`.cursor/rules/04-scope-guardrails.mdc` first, separately from style/readability notes —
those are the ones that block merge regardless of how small the diff is.
